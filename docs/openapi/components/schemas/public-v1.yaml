PublicV1Pagination:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1Pagination
PublicV1TournamentSummary:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1TournamentSummary
PublicV1StageSummary:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1StageSummary
PublicV1TournamentDetail:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1TournamentDetail
PublicV1Match:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1Match
PublicV1MatchGame:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1MatchGame
PublicV1MatchDetail:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1MatchDetail
PublicV1Standing:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1Standing
PublicV1ArbitrationMetrics:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1ArbitrationMetrics
PublicV1TournamentArbitration:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1TournamentArbitration
PublicV1TeamMember:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1TeamMember
PublicV1Team:
  # Généré depuis lib/apiContracts/public/v1 (type TS du handler vérifié par test).
  x-zod: response.PublicV1Team
